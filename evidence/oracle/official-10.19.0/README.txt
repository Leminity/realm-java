# Official Realm Java 10.19.0 artifact oracle

Captured at `2026-07-16T02:53:23Z` from
`https://repo1.maven.org/maven2/io/realm/`.

## Verify from the repository root

```sh
sha256sum --check evidence/oracle/official-10.19.0/checksums/sha256sum.txt
```

The manifest intentionally uses repository-root-relative paths, so run this command
from the repository root, not from this evidence directory. The matching Maven Central
SHA-1 sidecars are stored beside it for independent verification by the Python verifier
recorded in task 8's fixture oracle.
