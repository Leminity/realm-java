# Native subagent delegation evidence

- Requested policy: up to three bounded `gpt-5.6-terra` probes before serial repository inspection.
- Adapted native role-intent command attempted before repository edits.
- Result: rejected with `parent_not_active_leader` because this session is an OMX team worker.
- The native collaboration surface exposes neither `agent_type` nor a model selector.
- Subagent skip reason: spawning would violate the required pre-validated role-intent contract; direct inspection was bounded to the workflow/test scope and is safer than fabricating routing metadata.
