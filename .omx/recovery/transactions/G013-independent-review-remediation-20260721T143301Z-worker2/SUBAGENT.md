# Delegation evidence

Subagent skip reason: adapted role-intent write is rejected in team-worker threads (parent_not_active_leader), while the available native surface exposes neither agent_type nor model override; spawning would have violated the routing/model contract.
