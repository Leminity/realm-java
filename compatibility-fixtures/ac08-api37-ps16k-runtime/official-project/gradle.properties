# Matches the immutable official fixture generator's AndroidX settings. This is scoped to
# the isolated upstream baseline build; it does not alter the fork toolchain or repository.
android.useAndroidX=true
android.suppressUnsupportedCompileSdk=36
